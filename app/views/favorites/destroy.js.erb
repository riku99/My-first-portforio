<% if @discovery.present? %>
$("#favorite-<%= @discovery.id %>").html("<%= escape_javascript(render('shared/favorite', object: @discovery)) %>");
<% else %>
$("#favorite-comment-<%= @comment.id %>").html("<%= escape_javascript(render('shared/favorite_come', { object: @comment, comment: 'comment'})) %>");
<% end %>
