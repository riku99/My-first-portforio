<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, object.id %></div>
  <div><%= hidden_field_tag :follow_page, page %></div>
  <div><%= hidden_field_tag :user_id, user_id %></div>
  <%= f.submit "フォローする", class: follow_view %>
<% end %>
