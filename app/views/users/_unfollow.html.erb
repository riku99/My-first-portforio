<%= form_for(current_user.active_relationships.find_by(followed_id: object.id), html: { method: :delete }, remote: true) do |f| %>
  <div><%= hidden_field_tag :follow_page, page %></div>
  <div><%= hidden_field_tag :user_id, user_id %></div>
  <%= f.submit "フォロー中", class: follow_view %>
<% end %>
