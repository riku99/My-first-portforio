<!DOCTYPE html>
<html>
  <head>
    <title>Toeic</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
    <!-- htmlが読み込まれる前にmenuが読み込まれるとエラーになるのでdeferを指定する -->
    <%= javascript_pack_tag 'menu', defer: "defer" %>
    <%= javascript_pack_tag 'flash', defer: "defer" %>

    <title><%= full_title(yield(:title)) %></title>

    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  </head>

  <body>
    <header>
      <div class="logo-title">
          <span>ELH</span>
      </div>
          <% if logged_in? %>
            <div id="side_menu_zone"></div>
          <% else %>
            <%= link_to "ログイン", login_path, class: "header-link h-login" %>
          <% end %>
    </header>
    <div id="flash_info">
    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: key, id: "info", "v-if" => "timerInfo") %>
    <% end %>
  </div>
      <%= yield %>
  </body>
</html>
